import React from 'react';
import { Title, Text, Button } from '@mantine/core';

const Home = () => {
  return (
    <div>
      <Title>Welcome to TaskP</Title>
      <Text>This is the home page of TaskP.</Text>
      <Button>Get Started</Button>
    </div>
  );
};

export default Home;
